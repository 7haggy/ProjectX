$('.first').cycle({
    fx: 'scrollHorz',
    speed: 'fast',
    timeout:  0,
    prev: '#fBack',
    next: '#fNext'
});
$('.second').cycle({
    fx: 'scrollHorz',
    speed: 'fast',
    timeout:  0,
    prev: '#sBack',
    next: '#sNext'
});
